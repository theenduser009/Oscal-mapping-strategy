# Source One mapping progress — September 10, 2026

Latest addition: [AR acceptance and thirteen-field extension](#after-report-update--ar-acceptance-and-thirteen-field-extension).

As of 5:00 PM Eastern. Scope follows the approved Excel source, model, target
path, mapping type and Notes. SSP's mapped-scope run and JSON assembly are
accepted. Assessment Results is the next selected model, but no AR mapping
handler or live run is complete.

## What changed today

- **SSP components:** approved partial hydration was implemented and live
  accepted at `system-security-plan.system-implementation.components[]`.
  The lookup supplied 1,435 rows: 1,428 interconnections and seven software
  records. Descriptions were available for 968 interconnections and all seven
  software records. [Hydration evidence](../checkpoints/2026-09-10_ssp_component_hydration_run.md).
- **SSP routing and date conversion:** corrected eight approved extension-property
  routes to `system-security-plan.system-characteristics.props[]`, and implemented
  `ATOIATO_DATE` → `system-security-plan.system-characteristics.date-authorized`
  using the documented timestamp-to-date rule, without timezone shifts.
  Earlier dispatch failures were followed by a successful live graph run.
  The property routes are accepted in that release; the later-discovered
  `PACKAGE_TYPE` naming correction remains open.
  [Accepted release](../checkpoints/2026-09-10_ssp_date_property_run_accepted.md).
- **SSP assembly:** assembled 2,813 mapped-scope documents at root
  `system-security-plan`, consuming 70,102 nodes and 67,289 edges. Duplicate and
  dangling-key checks passed; database writes stayed disabled.
  This is transient notebook JSON, not a persisted export or full/schema-valid SSP.
  [Assembly evidence](../checkpoints/2026-09-10_ssp_mapped_scope_assembly_accepted.md).
- **Assessment Results preparation:** grouped the newly posted CSV by literal
  path/type/Notes. Its 45 AR-target row occurrences represent 44 distinct field
  names, including one duplicated score field. Four groups are documented below.
  No AR fields were implemented or live accepted by this preparation.
- **Tracking:** published a model/path register with status, evidence and
  clarification needs. The latest repository suite passed 212 local tests,
  including nine grouping tests. Test counts are not mapping-completion counts.

## Completed-so-far evidence

| Scope | What is established |
| --- | --- |
| SSP implementation inventory | 43 distinct Archer fields / 44 source-to-target entries: metadata 11, system characteristics 27, component references 6. One field is reused for metadata title. This is implemented-route inventory, not 44 fully verified Excel rows. It includes the package-type route whose name needs correction. |
| Metadata mapping scope | Accepted releases include timestamps, document identifiers, approved role/party assignments and support objects. Controlled document/OSCAL versions are not additional Archer-field rows. |
| Individual payload evidence | Document identifiers match source values for all 2,813 records; configured OSCAL version also passed checks for all 2,813. This does not establish every other field's payload equality. |
| Component-reference scope | All six Excel-defined reference routes are implemented; approved partial hydration is accepted. No separate component-status mapping is evidenced. |
| Assessment Results | Zero implemented AR mappings and zero accepted AR runs. Grouping is preparation only. |

See the [field-to-model/path register](../MAPPING_PROGRESS.md) and
[metadata payload evidence](../checkpoints/2026-09-09_CELL7_CELL8_OUTPUT_CHECKPOINT.md).
No whole-workbook completion percentage is asserted: the original full workbook
has not been reconciled, and the new CSV is a 51-record transcription subset.

## Pending work and blockers

| Model / exact scope | Remaining work |
| --- | --- |
| Assessment Results: `assessment-results.results[].observations[]` | 20 row occurrences / 19 distinct score fields. Confirm how each observation carries the source score. Reconcile the duplicated `AVG_SECURITY_COMPLIANCE_SCORE` and the inherent-risk Notes discrepancy before treating the transcription as exact. |
| Assessment Results: literal `assessment-results.results[].observations[] or props[]` | 17 rows have an unresolved destination choice. Do not silently turn this into nested observation properties. |
| Assessment Results: `assessment-results.results[].props[]` | Seven workflow fields have conditional audit-trail Notes; inclusion and name/value rules remain unconfirmed. |
| Assessment Results: `assessment-results.results[].findings[]` | Reference identity, source shape and association with the parent result remain to be established. |
| SSP: `system-security-plan.system-characteristics.props[]` — parked | `HELPER_PTA_CALC` is incorrectly skipped; `PACKAGE_TYPE` needs its Notes-defined property name. These corrections are not finished by the accepted graph run. |
| SSP: `system-security-plan.system-characteristics.security-sensitivity-level` — parked | `SECURITY_CATEGORY` still needs code/artifact reconciliation; it is not a property-collection mapping. |
| SSP unresolved rules — parked | Recommended security category has no approved populated-value rule. Control Implementation's proposed count mapping still lacks an agreed executable contract. |
| Source gaps / conformance | Missing source descriptions/status/boundary data and hardware hydration remain; 460 interconnection descriptions are absent. Preserve timestamps as requested. Full SSP conformance is unproven. An unlisted component-status requirement is not an approved Excel work item. |

The owner's current direction parks SSP corrections and moves through other
Source One models. Profile, POA&M and Assessment Plan rows remain review
candidates, not completed releases.

## Next mapping

**Model:** Assessment Results. **Target:** `assessment-results.results[].observations[]`.
**First fields:** `VULNERABILITY_SCORE`, `ANTIVIRUS_SCORE`, `PATCH_SCORE`,
`SECURITY_COMPLIANCE_SCORE`. **Rule:** Archer-specific risk scores mapped as
observations, retaining source values rather than inventing scoring formulas.

One observation per field carrying its score as a named property is a
**proposal awaiting confirmation**, not an approved nested target. Settle that
representation, verify the relevant registry/identity contract, then implement
one reusable handler for matching rows. [Grouped work and questions](../ASSESSMENT_RESULTS_START_HERE.md).

No SSP rerun is needed for this report. It changes reporting documents only,
executes no Snowflake writes, and has not been sent to the manager.

## Historical after-report update — first Assessment Results implementation

After the 5 PM snapshot, the owner approved one observation per score field,
with its score in a named property inside the collection item. The first four
fields above are now implemented in a
[separate in-memory mapping cell](../../notebooks/assessment_results/01_map_observation_scores.py).
This is actual node/edge/payload generation, not the earlier grouping helper.
SSP cells and accepted outputs are unchanged. Four AR mappings are implemented;
none is yet live accepted or payload-proven in Snowflake. The other grouped rows
remain pending. Run only the new mapping cell after the existing inputs and
helpers are initialized, with writes disabled; see the
[run instructions](../ASSESSMENT_RESULTS_START_HERE.md).

## After-report update — AR acceptance and thirteen-field extension

The [posted Assessment Results run](../ssp_mapping_progress_checkpoint.md#assessment-results-mapped-scope-checkpoint--2026-09-10)
is now accepted: vulnerability, antivirus, patch and security-compliance scores
each emitted for all 2,813 records with zero missing/invalid values. It produced
16,878 nodes, 14,065 edges and 2,813 partial documents. Contract errors, duplicate
keys and dangling edges were zero. No database writes occurred.

The next production increment implements **thirteen additional fields** at
`assessment-results.results[].observations[]`, using the same approved
`Extension Property` / score-as-observation Notes. These include operating
environment, password age, reporting scores, package scores/grade and the
unambiguous average-score fields. The [exact field-to-property list](../ASSESSMENT_RESULTS_START_HERE.md#expanded-batch--thirteen-additional-fields-pending-live)
and [field register](../MAPPING_PROGRESS.md#assessment-results-field-register)
show every addition. Existing source values are preserved; no new average,
total or grade formula is calculated.
All 236 local tests pass, including first-four compatibility and expanded
payload/parent-key checks. A local CSV-backed check also passes with synthetic
source values; this is not a live Snowflake checkpoint for the new fields.

Current AR inventory is **45 rows: 4 live accepted, 13 implemented/pending live,
28 not enabled**. The duplicated average-security-compliance field, inherent-risk
Notes conflict, alternative destinations, conditional workflow properties and
finding references remain visible as separate unresolved items. The four-field
checkpoint does not validate the expanded release or the full AR model.

Next action: replace only the existing separate AR mapper cell with the refreshed
file, run once with writes disabled, and post its printed seventeen-field report.
No SSP, grouping-report, registry-setup or assembler rerun is required in the
active session. This update is prepared for manager review, not sent to a manager.
