-- AR target definition using the owner-approved SSP physical layout.
-- Run as SQL, outside a transaction. Creates missing tables only.
-- Existing tables/rows are untouched; IF NOT EXISTS does not correct their schema.
-- Compare existing definitions below and reconcile any mismatch before COMMIT.
CREATE TABLE IF NOT EXISTS RTX_ENTERPRISESERVICES_DEV.ES_ESC_GRC_CURATED.DIM_OSCAL_ASSESSMENT_RESULTS_ELEMENT (
    PK_DIM_OSCAL_ASSESSMENT_RESULTS_ELEMENT_HASH BINARY(16) NOT NULL,
    ELEMENT_TYPE VARCHAR(64),
    OSCAL_UUID VARCHAR(32),
    METADATA_JSON VARIANT,
    SOURCE_SYSTEM_NAME VARCHAR(100),
    SOURCE_TABLE_NAME VARCHAR(128),
    SOURCE_RECORD_ID VARCHAR(128),
    DW_PIPELINE_RUN_ID VARCHAR(64),
    DW_LOAD_TIMESTAMP TIMESTAMP_TZ(9),
    DW_LOAD_TIMESTAMP_TZ TIMESTAMP_TZ(9)
);

CREATE TABLE IF NOT EXISTS RTX_ENTERPRISESERVICES_DEV.ES_ESC_GRC_CURATED.FACT_OSCAL_ASSESSMENT_RESULTS_DEPENDENCY (
    PK_FACT_OSCAL_ASSESSMENT_RESULTS_DEPENDENCY_HASH BINARY(16) NOT NULL,
    FK_SOURCE_ELEMENT_HASH BINARY(16) NOT NULL,
    FK_TARGET_ELEMENT_HASH BINARY(16) NOT NULL,
    DEPENDENCY_TYPE VARCHAR(32) NOT NULL,
    SOURCE_OSCAL_UUID VARCHAR(32) NOT NULL,
    TARGET_OSCAL_UUID VARCHAR(32) NOT NULL
);

DESC TABLE RTX_ENTERPRISESERVICES_DEV.ES_ESC_GRC_CURATED.DIM_OSCAL_ASSESSMENT_RESULTS_ELEMENT;
DESC TABLE RTX_ENTERPRISESERVICES_DEV.ES_ESC_GRC_CURATED.FACT_OSCAL_ASSESSMENT_RESULTS_DEPENDENCY;
